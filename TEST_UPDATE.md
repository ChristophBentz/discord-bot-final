# Test-Marker für Update-Feature

Diese Datei existiert nur zum Testen des Dashboard-Update-Buttons.

**Nach erfolgreichem Test einfach löschen:**

```bash
rm TEST_UPDATE.md && git add -A && git commit -m "Test-Marker entfernt" && git push
```
